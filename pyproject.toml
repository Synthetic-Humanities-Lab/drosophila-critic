[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = ["vendor", ".venv", "data", "results"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
